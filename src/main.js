// Import Bootstrap CSS and JS
import * as bootstrap from 'bootstrap'
import 'bootstrap/dist/css/bootstrap.min.css'

// Import FontAwesome
import '@fortawesome/fontawesome-free/css/all.min.css'

// Import custom styles
import './style.css'

// Votre code JavaScript pour l'interactivité ici
console.log('Bootstrap version:', bootstrap.Modal.VERSION)

// Exemple : ajouter des événements
document.addEventListener('DOMContentLoaded', () => {
  console.log('Page chargée et Bootstrap prêt !')

  // Vous pouvez ajouter votre logique JavaScript ici
})
